project_id = "your-gcp-project-id"
region     = "asia-northeast1"
name       = "demomotion-ai"

# HTTP Basic auth for the web dashboard. Leave empty to disable the gate.
basic_auth_user = "demo"
basic_auth_pass = "change-me"

# GitHub repo allowed to federate into this project's CI (keyless auth).
github_repository = "your-org/your-repo"
